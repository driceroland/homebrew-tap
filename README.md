# Office Commun's Homebrew tap

[Search](https://officecommun.com/search), a browser for the Mac with nothing in the way.

```sh
brew install --cask driceroland/tap/search
```

Search updates itself; `brew upgrade` works too.
